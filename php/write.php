<?php
    // $myfile = fopen("newfile.txt", "w") or die("Unable to open file!");
    // $txt = "John Doe\n";
    // fwrite($myfile, $txt);
    // $txt = "Jane Doe\n";
    // fwrite($myfile, $txt);
    // fclose($myfile);

    $file = $_POST['file'];
    echo $file;
    $road = $_POST['road'];
    echo $road;
    $num = $_POST['num'];
    echo $num;
?>
